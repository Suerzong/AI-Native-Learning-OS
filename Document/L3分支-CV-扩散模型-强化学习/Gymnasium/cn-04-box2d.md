# Box2D

这些环境都涉及基于物理控制的玩具游戏，使用 box2d 物理引擎和基于 PyGame 的渲染。这些环境由 Oleg Klimov 在 OpenAI Gym 的早期贡献，此后成为流行的玩具基准。

可以通过以下方式安装此环境集的依赖：

    pip install swig
    pip install gymnasium[box2d]

SWIG 是构建 box2d-py（提供 box2d 绑定的 Python 包）的 wheel 所必需的。
