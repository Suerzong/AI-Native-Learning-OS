# 睡前复习报告
日期：2026-05-15
到期卡片：108 张

## 今日新学知识点
- [data-structures] 二叉树概念与遍历：二叉树概念与遍历（第二层：树与哈希，章节 15-17）
- [data-structures] 二叉搜索树 BST：二叉搜索树 BST（第二层：树与哈希，章节 18-19）

## 今日到期复习
1. **[data-structures] 递归** ⚠️错题
   任务：重写 fibonacci 函数，先写 `if (n <= 1) return n;` 再写递归调用

2. **[neural-networks] 前向传播** ⚠️错题
   任务：对每个矩阵运算先写出 shape，再决定用 @ 还是需要转置

3. **[neural-networks] 激活函数** ⚠️错题
   任务：两种公式分别实现，对比计算结果是否一致

4. **[neural-networks] Batch Normalization** ⚠️错题
   任务：手写 BN 训练/推理两套逻辑，对比 train/eval 下同一输入的不同输出

5. **[neural-networks] 损失函数 / 网络设计** ⚠️错题
   任务：给出 5 种任务场景，写出正确的输出层激活和损失函数组合

6. **[cpp] 函数新特性** ⚠️错题
   任务：写两个 swap 函数（指针版和引用版），对比调用处的语法差异

7. **[cpp] new/delete** ⚠️错题
   任务：写一个带构造/析构打印的类，分别用 new/delete 和 malloc/free 管理，观察输出差异

8. **[cpp] 类与对象** ⚠️错题
   任务：每次写完类定义后检查分号

## 错题回炉
- [data-structures] 递归：重写 fibonacci 函数，先写 `if (n <= 1) return n;` 再写递归调用
- [neural-networks] 前向传播：对每个矩阵运算先写出 shape，再决定用 @ 还是需要转置
- [neural-networks] 激活函数：两种公式分别实现，对比计算结果是否一致
- [neural-networks] Batch Normalization：手写 BN 训练/推理两套逻辑，对比 train/eval 下同一输入的不同输出
- [neural-networks] 损失函数 / 网络设计：给出 5 种任务场景，写出正确的输出层激活和损失函数组合
- [cpp] 函数新特性：写两个 swap 函数（指针版和引用版），对比调用处的语法差异
- [cpp] new/delete：写一个带构造/析构打印的类，分别用 new/delete 和 malloc/free 管理，观察输出差异
- [cpp] 类与对象：每次写完类定义后检查分号

## 主动回忆问题
1. 【递归】你能正确说出：程序运行时栈溢出（Segmentation fault） 的正确做法吗？
2. 【前向传播】你能正确说出：写 `x @ W` 还是 `W @ x` 不确定；shape (3,) 和 shape (3,1)  的正确做法吗？
3. 【激活函数】你能正确说出：写 Sigmoid 导数为 e^{-x}/(1+e^{-x})^2 而不是 σ(x)(1-σ(x)) 的正确做法吗？
4. 【Batch Normalization】你能正确说出：问"推理时 BN 用谁的均值和方差"，回答"在嵌入式端后来的均值和方差"——即认为推理时用当前输入数 的正确做法吗？
5. 【损失函数 / 网络设计】你能正确说出：回归问题用 Softmax 输出、分类问题用线性输出 + MSE 的正确做法吗？

## 下次复习
- 队列中暂无待复习卡片

## 复习反馈
完成复习后请回复：`复习反馈：知识点=评分`
评分：1=完全忘了 2=模糊 3=基本记得 4=清晰
示例：`复习反馈：反向传播=4, 循环队列=2`