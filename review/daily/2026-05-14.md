# 睡前复习报告
日期：2026-05-14
到期卡片：102 张

## 今日到期复习
1. **[data-structures] 单链表** ⚠️错题
   任务：画出插入前后链表的内存状态，标注每个指针的赋值顺序

2. **[data-structures] 递归** ⚠️错题
   任务：重写 fibonacci 函数，先写 `if (n <= 1) return n;` 再写递归调用

3. **[data-structures] 队列** ⚠️错题
   任务：实现用 size 计数器区分空和满的循环队列

4. **[neural-networks] 前向传播** ⚠️错题
   任务：对每个矩阵运算先写出 shape，再决定用 @ 还是需要转置

5. **[neural-networks] 激活函数** ⚠️错题
   任务：两种公式分别实现，对比计算结果是否一致

6. **[neural-networks] Batch Normalization** ⚠️错题
   任务：手写 BN 训练/推理两套逻辑，对比 train/eval 下同一输入的不同输出

7. **[neural-networks] 损失函数 / 网络设计** ⚠️错题
   任务：给出 5 种任务场景，写出正确的输出层激活和损失函数组合

8. **[cpp] 函数新特性** ⚠️错题
   任务：写两个 swap 函数（指针版和引用版），对比调用处的语法差异

## 错题回炉
- [data-structures] 单链表：画出插入前后链表的内存状态，标注每个指针的赋值顺序
- [data-structures] 递归：重写 fibonacci 函数，先写 `if (n <= 1) return n;` 再写递归调用
- [data-structures] 队列：实现用 size 计数器区分空和满的循环队列
- [neural-networks] 前向传播：对每个矩阵运算先写出 shape，再决定用 @ 还是需要转置
- [neural-networks] 激活函数：两种公式分别实现，对比计算结果是否一致
- [neural-networks] Batch Normalization：手写 BN 训练/推理两套逻辑，对比 train/eval 下同一输入的不同输出
- [neural-networks] 损失函数 / 网络设计：给出 5 种任务场景，写出正确的输出层激活和损失函数组合
- [cpp] 函数新特性：写两个 swap 函数（指针版和引用版），对比调用处的语法差异

## 主动回忆问题
1. 【单链表】你能正确说出：在中间位置插入新节点后，后面的节点丢失 的正确做法吗？
2. 【递归】你能正确说出：程序运行时栈溢出（Segmentation fault） 的正确做法吗？
3. 【队列】你能正确说出：入队时误判队列已满，或空队列时出队 的正确做法吗？
4. 【前向传播】你能正确说出：写 `x @ W` 还是 `W @ x` 不确定；shape (3,) 和 shape (3,1)  的正确做法吗？
5. 【激活函数】你能正确说出：写 Sigmoid 导数为 e^{-x}/(1+e^{-x})^2 而不是 σ(x)(1-σ(x)) 的正确做法吗？

## 下次复习
- 队列中暂无待复习卡片

## 复习反馈
完成复习后请回复：`复习反馈：知识点=评分`
评分：1=完全忘了 2=模糊 3=基本记得 4=清晰
示例：`复习反馈：反向传播=4, 循环队列=2`