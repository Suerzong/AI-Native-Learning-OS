# 睡前复习报告
日期：2026-05-23
到期卡片：122 张

## 今日到期复习
1. **[neural-networks] 激活函数** ⚠️错题 | 第3次复习
   任务：两种公式分别实现，对比计算结果是否一致

2. **[neural-networks] Batch Normalization** ⚠️错题 | 第3次复习
   任务：手写 BN 训练/推理两套逻辑，对比 train/eval 下同一输入的不同输出

3. **[neural-networks] 损失函数 / 网络设计** ⚠️错题 | 第3次复习
   任务：给出 5 种任务场景，写出正确的输出层激活和损失函数组合

4. **[cpp] 函数新特性** ⚠️错题 | 第3次复习
   任务：写两个 swap 函数（指针版和引用版），对比调用处的语法差异

5. **[cpp] new/delete** ⚠️错题 | 第3次复习
   任务：写一个带构造/析构打印的类，分别用 new/delete 和 malloc/free 管理，观察输出差异

6. **[cpp] 类与对象** ⚠️错题 | 第3次复习
   任务：每次写完类定义后检查分号

7. **[cpp] 循环** ⚠️错题
   任务：写一个程序分别用引用和不用引用修改 vector 元素，对比结果

8. **[cpp] 多态** ⚠️错题
   任务：写一个基类/派生类，基类析构函数不加 virtual，通过基类指针 delete，观察输出；然后加 virtual 对比

## 错题回炉
- [neural-networks] 激活函数：两种公式分别实现，对比计算结果是否一致
- [neural-networks] Batch Normalization：手写 BN 训练/推理两套逻辑，对比 train/eval 下同一输入的不同输出
- [neural-networks] 损失函数 / 网络设计：给出 5 种任务场景，写出正确的输出层激活和损失函数组合
- [cpp] 函数新特性：写两个 swap 函数（指针版和引用版），对比调用处的语法差异
- [cpp] new/delete：写一个带构造/析构打印的类，分别用 new/delete 和 malloc/free 管理，观察输出差异
- [cpp] 类与对象：每次写完类定义后检查分号
- [cpp] 循环：写一个程序分别用引用和不用引用修改 vector 元素，对比结果
- [cpp] 多态：写一个基类/派生类，基类析构函数不加 virtual，通过基类指针 delete，观察输出；然后加 virtual 对比

## 主动回忆问题
1. 【激活函数】你能正确说出：写 Sigmoid 导数为 e^{-x}/(1+e^{-x})^2 而不是 σ(x)(1-σ(x)) 的正确做法吗？
2. 【Batch Normalization】你能正确说出：问"推理时 BN 用谁的均值和方差"，回答"在嵌入式端后来的均值和方差"——即认为推理时用当前输入数 的正确做法吗？
3. 【损失函数 / 网络设计】你能正确说出：回归问题用 Softmax 输出、分类问题用线性输出 + MSE 的正确做法吗？
4. 【函数新特性】你能正确说出：认为 `int& r = x;` 和 `int* p = &x;` 是一回事，调用时不知道什么时候用 的正确做法吗？
5. 【new/delete】你能正确说出：用 `new` 分配后用 `free` 释放，或用 `malloc` 后用 `delete` 的正确做法吗？

## 下次复习
- 队列中暂无待复习卡片

## 复习反馈
完成复习后请回复：`复习反馈：知识点=评分`
评分：1=完全忘了 2=模糊 3=基本记得 4=清晰
示例：`复习反馈：反向传播=4, 循环队列=2`