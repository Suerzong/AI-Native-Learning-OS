# 睡前复习报告
日期：2026-05-13
到期卡片：90 张

## 今日新学知识点
-  完成了栈的学习，循环队列还需要再练：完成了栈的学习，循环队列还需要再练

## 今日到期复习
1. **[cpp] 类与对象** ⚠️错题
   任务：每次写完类定义后检查分号

2. **[cpp] 多态** ⚠️错题
   任务：写一个基类/派生类，基类析构函数不加 virtual，通过基类指针 delete，观察输出；然后加 virtual 对比

3. **[cpp] 函数模板 / 类模板** ⚠️错题
   任务：把模板定义移到头文件中，重新编译

4. **[data-structures] 单链表** ⚠️错题
   任务：画出插入前后链表的内存状态，标注每个指针的赋值顺序

5. **[data-structures] 快速排序** ⚠️错题
   任务：手动模拟 partition([3,1,4,1,5,9,2,6]) 的每一步

6. **[data-structures] 二分查找** ⚠️错题
   任务：写出两种边界写法（`left <= right` 和 `left < right`），分别适用什么场景

7. **[data-structures] 递归** ⚠️错题
   任务：重写 fibonacci 函数，先写 `if (n <= 1) return n;` 再写递归调用

8. **[data-structures] 队列** ⚠️错题
   任务：实现用 size 计数器区分空和满的循环队列

## 错题回炉
- [cpp] 类与对象：每次写完类定义后检查分号
- [cpp] 多态：写一个基类/派生类，基类析构函数不加 virtual，通过基类指针 delete，观察输出；然后加 virtual 对比
- [cpp] 函数模板 / 类模板：把模板定义移到头文件中，重新编译
- [data-structures] 单链表：画出插入前后链表的内存状态，标注每个指针的赋值顺序
- [data-structures] 快速排序：手动模拟 partition([3,1,4,1,5,9,2,6]) 的每一步
- [data-structures] 二分查找：写出两种边界写法（`left <= right` 和 `left < right`），分别适用什么场景
- [data-structures] 递归：重写 fibonacci 函数，先写 `if (n <= 1) return n;` 再写递归调用
- [data-structures] 队列：实现用 size 计数器区分空和满的循环队列

## 主动回忆问题
1. 【类与对象】你能正确说出：`class Foo { ... }` 后面没有 `;` 的正确做法吗？
2. 【多态】你能正确说出：通过基类指针 `delete` 派生类对象时，派生类的析构函数没有被调用 的正确做法吗？
3. 【函数模板 / 类模板】你能正确说出：链接时报 undefined reference 的正确做法吗？
4. 【单链表】你能正确说出：在中间位置插入新节点后，后面的节点丢失 的正确做法吗？
5. 【快速排序】你能正确说出：partition 后数组没有被正确分为"小于 pivot"和"大于 pivot"两部分 的正确做法吗？

## 明日继续
- [cpp] 函数新特性
- [cpp] new/delete
- [cpp] 循环
- [neural-networks] 反向传播
- [neural-networks] 注意力机制

## 复习反馈
完成复习后请回复：`复习反馈：知识点=评分`
评分：1=完全忘了 2=模糊 3=基本记得 4=清晰
示例：`复习反馈：反向传播=4, 循环队列=2`