# 每日诊断短测

每日短测用于判断今天应该训练什么，而不是为了打分。

## 模板

```md
## YYYY-MM-DD

### 诊断题

1. 概念题：
2. 语法题：
3. 修改题：
4. 调试题：

### 结果

- 正确率：
- 主要错误：
- 今日训练目标：
- 是否允许推进：
```

---

## 样例：C++ 基础（对应第一层）

### 诊断题

1. 概念题：`auto` 和 `dynamic_cast` 有什么区别？`auto` 是动态类型吗？
2. 语法题：`int& r;` 能编译通过吗？为什么？
3. 修改题：以下代码有什么问题？如何修复？
   ```cpp
   int* p = new int(42);
   free(p);
   ```
4. 调试题：编译时报 `error: 'cout' is not a member of 'std'`，最可能的原因是什么？

### 结果

- 正确率：待测
- 主要错误：待记录
- 今日训练目标：待定
- 是否允许推进：待定

---

## 样例：面向对象（对应第二层）

### 诊断题

1. 概念题：`class` 和 `struct` 在 C++ 中有什么区别？什么时候用哪个？
2. 语法题：在类外部定义成员函数，语法是什么？写出 `class Foo` 中 `void bar()` 的外部定义。
3. 修改题：以下继承中，派生类能访问基类的 private 成员吗？
   ```cpp
   class Base { private: int x; };
   class Derived : public Base { void f() { x = 1; } };
   ```
4. 调试题：通过基类指针 delete 派生类对象时，只调用了基类的析构函数，没有调用派生类的析构函数。原因是什么？

### 结果

- 正确率：待测
- 主要错误：待记录
- 今日训练目标：待定
- 是否允许推进：待定

---

## 样例：STL（对应第三层）

### 诊断题

1. 概念题：`vector` 和 `list` 的主要区别是什么？什么场景下用哪个？
2. 语法题：如何用 `sort` 对 `vector<int>` 降序排序？
3. 修改题：以下代码遍历 vector 时删除元素，有什么问题？
   ```cpp
   for (auto it = v.begin(); it != v.end(); ++it) {
       if (*it == 0) v.erase(it);
   }
   ```
4. 调试题：`shared_ptr` 的循环引用问题是什么？如何解决？

### 结果

- 正确率：待测
- 主要错误：待记录
- 今日训练目标：待定
- 是否允许推进：待定

---

## 样例：工程实战（对应第四层）

### 诊断题

1. 概念题：CMake 的 `target_link_libraries` 和 `link_libraries` 有什么区别？
2. 语法题：写出一个最简单的 CMakeLists.txt，编译一个 `main.cpp` 为可执行文件 `myapp`。
3. 修改题：交叉编译时报找不到头文件，最可能的原因是什么？
4. 调试题：多线程程序出现数据竞争（data race），用什么机制保护共享数据？

### 结果

- 正确率：待测
- 主要错误：待记录
- 今日训练目标：待定
- 是否允许推进：待定
