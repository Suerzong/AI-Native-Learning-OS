## 第 1 页

第3章 栈和队列

---

## 第 2 页

3.4 　栈与递归
递归的定义  若一个对象部分地包含它自己,  或用它自己给自己定义,  则称这个对象是递归的；若一个过程直接地或间接地调用自己, 则称这个过程是递归的过程。
直接调用；
long Fact ( long n )
{
  if ( n == 0) return 1;
  else
   return n * Fact (n-1);
}
间接调用；
long Fact ( long n ) {
```c
    if ( n == 0) return 1;
    else return  FactB(n);
```
}
long Fact B( long n ) {
  return n*Fact(n-1)
}
n! = n × (n-1) × (n-2) × ... × 1

---

## 第 3 页

链式递归调用：五个子函数和main函数
int fun1(int n)
{
     if(n==0)
	return 1;
     else
	return n*fun2(n-1);
}
int fun2(int n)
{
	if(n==0)
		return 1;
	else
		return n*fun3(n-1);
}
int fun3(int n)
{
	if(n==0)
		return 1;
	else
		return n*fun4(n-1);
}
int fun4(int n)
{
     if(n==0)
	return 1;
     else
	return n*fun5(n-1);
}
int fun5(int n)
{
	if(n==0)
		return 1;
	else
		return 1;
}
void main()
{
	int i;
	i = fun1(4);
	printf(“%d”, i);
}
当被调用的fun5执行到n==0条件成立时，此时在调用栈中可以看到从函数fun1到函数fun5的依次调用次序

---

## 第 4 页

执行fun1(4)时，会有什么样的调用和返回？

---

## 第 5 页

代码区
静态数据区
动态数据区
利用主进程的栈，为函数调用返回时做工作记录，分成三个部分：
返回地址  实在参数表(变参和值参)   局部变量
发生调用时，保护现场：即当前的工作记录入栈，然后转入被调用的过程(绿色部分是调用时新入栈的内容)
一个调用结束时，恢复现场：即若栈不空，则退栈(退到蓝色部分的结束位置)，从退出的返回地址处继续执行下去
调用者正在
使用的栈
的内容，

从函数返回后
要从这里继续
使用，需保存
这个栈顶值
函数的
参数区

最后一个
参数
先入栈
返回调用者后的
下一条指令地址
调用者栈顶地址
被调用函数
从这里开始使用
主进程的栈

---

## 第 6 页

1个子函数和main函数
int fun1(int n)
{
	if(n==0)
		return 1;
	else
		return n*fun1(n-1);
}
void main()
{
	int i;
	i = fun1(4);
	printf(“%d”, i);
}
当被调用的fun1执行到n==0条件成立时，此时在调用栈中可以看到函数fun1的依次调用次序

---

## 第 7 页

1. 递归(Recursion)定义的数学函数:
阶乘函数:
2阶Fibonaci数列函数:
基础部分
递归部分
基础部分
递归部分
基础部分：
不用递归直接求解的部分，也可称为“结束条件”。

递归部分：
重复应用可将右侧表达式转变为基础部分

必要条件：递归部分的每次应用可以让我们更接近基础部分，即函数的参数值每递归一次都接近基础部分（上面两个函数都是以递减的方式接近）。

---

## 第 8 页

---

## 第 9 页

    递归是一个过程或函数直接或间接调用自身的一种方法，它可以把一个大型的问题层层转化为一个与原问题相似、但规模较小的问题来求解。
能够用递归解决的问题应该满足以下三个条件：
需要解决的问题可以化为一个或多个子问题来求解，而这些子问题的求解方法与原来的问题完全相同，只是在数量规模上不同（减少规模）；
递归调用的次数必须是有限的；
必须有结束递归的条件（边界条件）来终止递归。
一个注意：递归算法本身不可以作为独立的程序运行，需在其它的程序中设置调用初值，进行调用；

---

## 第 10 页

求解递归问题算法的一般形式：
void   p (参数表)
{
    if (递归结束条件为真)
        直接求解；                                              -----基本项，基础部分
    else
    {
          从参数计算出问题规模缩减后的新参数
          p（规模缩减后的新参数）；              ------归纳项，递归部分
     }
}
long Fact ( long n )
{
```c
    if ( n == 0)  return 1;                  //基本项，基础部分
    else return  n * Fact (n-1);          //归纳项，递归部分
```
}

---

## 第 11 页

求阶乘的问题，假设程序运行时，n＝4，那么程序的执行过程如下：

---

## 第 12 页

  从上面可以看出，递归调用的过程分为两个阶段：
      1）递归过程：将原始问题不断转化为规模小了一级的新问题，从求4！变成求3！，变成求2！，最终达到递归终结条件，求1！；
      2）回溯过程：从已知条件出发，沿递归的逆过程，逐一求值返回，直至递归初始处，完成递归调用。

---

## 第 13 页

返回 2　参数值为 2    计算 2*Fact(1)
返回 1　参数值为 1    计算 1*Fact(0)
返回 1　参数值为 0    直接定值 = 1
```c
if ( n == 0 ) return 1;
else return n * Fact (n-1);
```
求解阶乘 n! 的过程
主程序 main : Fact(4)
返回 6　参数值为 3    计算 3*Fact(2)

---

## 第 14 页

设有一个递归算法如下:
```c
int X(int n)
{ if(n<=3) return 1;
```
  else return X(n-2)+X(n-4)+1
}
则计算X(X(8))时需要计算X函数       次.
D
练习
A. 8   	       B.9          C.16 	   D.18
调用层次 |  |  |  |
--- | --- | --- | --- | ---
0 | 8 |  |  |
1 | 6 |  | 4 |
2 | 4 | 2 | 2 | 0
3 | 2 | 0 |  |
先计算X(8)，得9
再计算X(9)
调用层次 |  |  |  |  |  |
--- | --- | --- | --- | --- | --- | ---
0 | 9 |  |  |  |  |
1 | 7 |  |  |  | 5 |
2 | 5 |  | 3 |  | 3 | 1
3 | 3 | 1 |  |  |  |
3
1
1
1
1
1
5
3
9

---

## 第 15 页

练习
调用层次 |  |  |  |
--- | --- | --- | --- | ---
0 | 8 |  |  |
1 | 6 |  | 4 |
2 | 4 | 2 | 2 | 0
3 | 2 | 0 |  |
先计算X(8)，得9
3
1
1
1
1
1
5
3
9
{X(n) = {1, if n<=3; X(n-2)+X(n-4)+1, else.}

---

## 第 16 页

练习
{X(n) = {1, if n<=3; X(n-2)+X(n-4)+1, else.}
再计算X(9)
调用层次 |  |  |  |  |  |
--- | --- | --- | --- | --- | --- | ---
0 | 9 |  |  |  |  |
1 | 7 |  |  |  | 5 |
2 | 5 |  | 3 |  | 3 | 1
3 | 3 | 1 |  |  |  |

---

## 第 17 页

规则:
(1) 每次只能移动一个圆盘
(2) 圆盘可以插在A,B和C中的任一塔座上
(3) 任何时刻不可将较大圆盘压在较小圆盘之上
Hanoi塔问题
在印度圣庙里，一块黄铜板上插着三根宝石针。
主神梵天在创造世界时，在其中一根针上穿好了由大到小的64片金片，这就是汉诺塔。
僧侣不停移动这些金片，一次只移动一片，小片必在大片上面。
当所有的金片都移到另外一个针上时，世界将会灭亡。

---

## 第 18 页

Hanoi塔问题

开始状态：
盘子在A柱上，B空，C空；
(1)如果可以将n-1个按大小要求挪动到B
(2)就可以将 A的最后一个直接移到 C
(3)问题规模缩减：将B上的n-1个盘子挪到C

基本部分：
当前A柱子上只有一个盘时，将其移动到C

递归部分：
```c
当前A柱子上有N个盘子，( n,a->c,b) 拆成：
1）做 (n-1, a->b, c)，即由a到b，借助c
```
2）将a上剩余的1个盘移动到c上，
3）再做 (n-1, b->c, a)，即由b到c，借助a

开始状态：
盘子在A柱上，B空，C空；
盘子在某柱上，另外一个空，C已经整理好；
开始(n个,A->C,借助B)
最后
先做(n-1,A->B,借助C)
再做(move(A, n, C)
再做(n-1,B->C,借助A)

---

## 第 19 页

基础部分：
不用递归直接求解的部分

递归部分：
是不断重复，最终可将要计算的内容转变为基础部分

必要条件：递归部分的每次应用可以让我们更接近基础部分，即函数的参数值每递归一次都接近基础部分

---

## 第 20 页

#include<iostream.h>
int c=0;\\记录移动步数
void move(char x,int n,char z)
{cout<<++c<<","<<n<<","<<x<<","<<z<<endl;}
\*每次调用move时，步数c加1，输出步数、盘子编号、起始和目标柱
void Hanoi(int n,char A,char B,char C)
{
    if(n==1) move(A,1,C);//基础部分
    else
    {    //递归部分
         Hanoi(n-1,A,C,B); //由a到b，借助c
         move(A,n,C);      //将a上剩余的1个盘移动到c上
         Hanoi(n-1,B,A,C); //由b到c，借助a
    }
}
void main()
{
	Hanoi(3,'a','b','c’);
}
跟踪程序，给出下列程序的运行结果，以深刻地理解递归的调用和返回过程

---

## 第 21 页

void hanoi(3,a,b,c)
{  if 3==1
         move(a,1,c)
    else
         {    hanoi(2,a,c,b);
              move(a,3,c);
              hanoi(2,b,a,c); }
 }
void hanoi(2,a,c,b)
{   if 2==1
           move(a,1,b)
    else
         {    hanoi(1,a,b,c);
              move(a,2,b);
              hanoi(1,c,a,b) }
 }
void hanoi(1,a,b,c)
{   if 1==1
            move(a,1,c) ;
    else    {……}
}
void hanoi(1,c,a,b)
{   if 1==1
            move(c,1,b) ;
    else   {…… }
}
void hanoi(2,b,a,c)
{   if 2==1
          move(b,1,c)
    else
         {   hanoi(1,b,c,a);
              move(b,2,c);
              hanoi(1,a,b,c); }
}
void  hanoi(1,b,c,a)
{  if 1==1
          move(b,1,a) ;
    else    {…… }
}
void hanoi(1,a,b,c)
{  if 1==1
          move(a,1,c) ;
    else    {…… }
}
a         b           c

---

## 第 22 页

3,a,b,c
3盘子的递归调用树
```c
⑤ a->c
① a->c
```

---

## 第 23 页

2盘子的递归调用树
2,a,b,c
④ 1,a,c,b
⑥ 1,b,a,c
```c
⑤,a->c
①,b->c
①,a->c
```
每次递归调用分解为Hanoi(n-1)，递归树的深度为n

---

## 第 24 页

蓝色  ：调用顺序
虚线  ：返回关系
黑色线：实际执行顺序

---

## 第 25 页

n：      1         2             3             4
Moves:  H(1)=1 H(1)+1+H(1)=3 H(2)+1+H(2)=7 H(3)+1+H(3)=15
H(n)   = 2H(n-1)+1
H(n-1) = 2H(n-2)+1
H(n-2) = 2H(n-3)+1
......
H(3)   = 2H(2)  +1
H(2)   = 2H(1)  +1
H(1)   =         1
H(n) = 20+21+…+2n-2+2n-1H(1)
     = 20+21+…+2n-2+2n-1
     = 2n-1
汉诺塔递归算法效率分析
H(n) = 2H(n-1)      +1
     = 22H(n-2)+21    +1
     = 23H(n-3)+22+21+1
...

     = 2n-1H(1)+2n-2+…+1

---

## 第 26 页

在印度圣庙里，一块黄铜板上插着三根宝石针。
主神梵天在创造世界时，在其中一根针上穿好了由大到小的64片金片，这就是汉诺塔。
僧侣不停移动这些金片，一次只移动一片，小片必在大片上面。
当所有的金片都移到另外一个针上时，世界将会灭亡。 当完成这任务后，世界将毁灭，是真的吗？
汉诺塔
64片金片移动次数：264-1=18446744073709551615
假如每秒钟移动一次，共需多长时间呢？
一年大约有31536926秒，移完这些金片需要５８００多亿年
世界、梵塔、庙宇和众生都已经灰飞烟灭 ……

---

## 第 27 页

空间效率
时间效率
O(2n)
与递归树的结点数成正比
与递归树的深度成正比
O(n)
汉诺塔递归算法效率分析

---

## 第 28 页

优点：结构清晰，理解递归概念后和待解决问题的规模缩减方法后，程序易读

缺点：每次调用要生成调用者程序的工作记录，保存调用者的状态信息，入栈；返回时要出栈，恢复调用者的状态信息。时间开销大。
递归的优缺点
优化：减少时间开销         递归非递归

1）算法改造，用递推或回溯解决
2）某些特殊的递归，可用栈模拟调用的方式改造为非递归算法

---

## 第 29 页

栈

---

## 第 30 页

以下三种情况常常用到递归方法
递归定义的数学函数
具有递归特性的数据结构
n问题的数据结构可拆解成为多个n-1的数据结构，如线性表，树
n长度的线性表=1个队首结点+n-1长度的线性表个结点
1个根n个树枝的树=1个树根结点+n棵树
递归求解更方便的问题，如汉诺塔、八王后等问题

---

## 第 31 页

1. 递归(Recursion)定义的数学函数:
阶乘函数:
2阶Fibonaci数列函数:
基础部分
递归部分
基础部分
递归部分
基础部分：
不用递归直接求解的部分，也可称为“结束条件”。

递归部分：
重复应用可将右侧表达式转变为基础部分

必要条件：递归部分的每次应用可以让我们更接近基础部分，即函数的参数值每递归一次都接近基础部分（上面两个函数都是以递减的方式接近）。

---

## 第 32 页

树
2. 具有递归特性的数据结构:
线性表
A线性表为：(a,b,c,d,e….) ，共 n个数据元素
递归分解为：
(a)+(b,c,d,e….)， 1个结点+n-1个结点
3. 递归求解更方便的问题:
　迷宫问题　Hanoi塔问题  八王后问题
a结点
加上
T1树、T2树 和 T3树
这三棵树继续可以递归分解

---

## 第 33 页

分治法：对于一个较为复杂的问题，能够分解成几个相对简单的且解法相同或类似的子问题来求解
1、能将一个问题转变成一个新问题，而新问题与原问题的解法相同或类同，不同的仅是处理的对象，且这些处理对象是变化有规律的，逐渐接近递归的边界（函数的结束条件）。

2、可以通过上述转化而使问题简化

3、必须有一个明确的递归出口，或称递归的边界
递归解法 是 一种分治法
必备的三个条件

---

## 第 34 页

分治法 求解递归问题算法的一般形式：
void   p (参数表)
{
    if (递归结束条件为真)
        直接求解；                                              -----基本项，基础部分
    else
    {
          从参数计算出问题规模缩减后的新参数
          p（规模缩减后的新参数）；              ------归纳项，递归部分
     }
}
long Fact ( long n )
{
```c
    if ( n == 0)  return 1;      //基本项，基础部分
    else return  n * Fact (n-1); //归纳项，递归部分
```
}

---

## 第 35 页

void conversion2(int n)
{
	if(n==0)	return ;
	else
	{

     }
}
void main()
{
     int n;	cin>>n;
	conversion2(n);
     cout<<endl;
}
递归练习1
conversion2(n/2);
cout<<n%2;
输入一个整数，输出对应的2进制形式
 1111001
cout<<n%2;
conversion2(n/2);
执行顺序：
递归分解 n 直到 n == 0。
回溯时依次输出余数（n % 2），自然得到二进制表示（高位到低位）。

---

## 第 36 页

void conversion2(int n)
{
   if(n>0)
   {
       conversion2(n/2);
       cout<<n%2;
   }
}
void main()
{
     int n;	cin>>n;
	conversion2(n);
     cout<<endl;
}
递归练习1
输入一个整数，输出对应的2进制形式
 1111001

---

## 第 37 页

数制转换
　void conversion2 ()　{  // 对于输入的任意非负十进制整数，输出与其等值的2进制数　　InitStack(S); 　　// 构造空栈　　scanf (“%d”,N);　　// 输入一个十进制数　　while(N)　　{　　　  Push(S, N % 2);　// “余数”入栈　　　  N = N/2;　　　　// 非零“商”继续运算　　} // while　　while (!StackEmpty(S))　　{ 　 // 和“求余”所得相逆的顺序输出八进制的各位数　　　  Pop(S,e);　　　  printf (“ %d”,e);　　} // while
    DestroyStack(S);　} // conversion2
使用栈的基本操作解决问题层次更清晰。

---

## 第 38 页

（10）已知f为有头结点的单链表的表头指针, 链表中存储的都是整型数据，试写出实现下列运算的递归算法：
① 求链表中的最大整数；
② 求链表的结点个数；
③ 求所有整数的平均值。
算法设计题

---

## 第 39 页

void main( )
{
    LinkList L;
    InitList(L);
    CreatList(L);
    if(L->next）
        cout<<"链表中的最大整数为:"
            <<GetMax(L->next) << endl;
    DestroyList(L);
}
```c
int GetMax(LinkList p){//求链表中的最大整数
    if(!p)  return INT_MIN;
    if(!p->next)
        return p->data;
```
	else
	{
```c
		 int max=GetMax(p->next);
		 return  p->data>=max ? p->data : max;
```
	}
}
从头结点的下一个开始
递归实现

---

## 第 40 页

int GetMax(LinkList p){//求链表中的最大整数
{
```c
    if(!p) return INT_MIN;
    Stack S；LinkList q=p->next;
    InitStack(S); 　　// 构造空栈    while(q)     //入栈，直到递归的基本项的条件满足    {        Push(S, q->data);　// 节点值入栈        q = q->next;　　　 // 向后移动　　} // while　　int e, max = INT_MIN;
    while (!StackEmpty(S))  //出栈，按递归部分的处理逻辑处理　　{ 　 // 从栈中依次弹出，找最大值        Pop(S,e);        max = max >= e ? max : e;
```
　　} // while
    DestroyStack(S);
    return max;
}
用栈模拟递归实现

---

## 第 41 页

void main( )
{
    LinkList L;
    InitList(L);
    CreatList(L);
    if(L->next）
        cout<<"链表中的最大整数为:"
            <<GetMax(L->next) << endl;
    DestroyList(L);
}
```c
int GetMax(LinkList p){//求链表中的最大整数
    if(!p)  return INT_MIN;
    ListList q = p->next;	int max= INT_MIN;
```
    while(q){
```c
        max = q->data>=max ? q->data : max;
        q = q->next;
```
    }
    return max;
}
递推实现
从头结点的下一个开始

---

## 第 42 页

1. 掌握栈和队列的特点，并能在相应的应用问题中正确选用
2. 熟练掌握栈的顺序栈和链栈的进栈出栈算法，特别应注意栈满和栈空的条件
3. 熟练掌握循环队列和链队列的进队出队算法，特别注意队满和队空的条件
4. 理解递归算法执行过程中栈的状态变化过程
5. 掌握表达式求值 方法
小结

---
