## 第 1 页

第1章 绪论
数据结构与算法
刘娜
18801176346
na.liu@bupt.edu.cn
网络技术中心
计算机学院（国家示范性软件学院）

---

## 第 2 页

考核方式
1. 平时成绩 :
考勤，作业；
课堂纪律
不要无故迟到
不要无故旷课
2. 期中成绩：考试
3. 期末成绩：考试

---

## 第 3 页

教材和参考书
教材：
《数据结构》(第2版)，严蔚敏，李冬梅等，人民邮电出版社
           http://www.ptpress.com.cn/book.aspx?id=40535

参考书：
《数据结构习题解析与实验指导》， 严蔚敏，数据结构配套用书
《数据结构、算法与应用——C++语言描述》，Sartaj Sahni 机械工业出版社
《数据结构与算法》，张晓丽，机械工业出版社
数据结构题集(C语言版)；严蔚敏等；清华大学出版社

---

## 第 4 页

为什么要学习数据结构？
   1. 打好编程基础和基础编程能力的训练
   2. 应用计算机程序解决工程问题时，对需要保 存和处理的数据：
分清数据的内在逻辑关系
     互不关联，一对一，一对多，多对多等
根据逻辑关系合理组织数据，并选择合理的存储方式存储数据
逻辑表示：表、树、图；
存储方式：顺序存储、链式存储
有效处理数据
选择快速、复杂度低的数据结构和算法解决问题

---

## 第 5 页

课程学习指导
1. 提前预习、认真听课、按时完成书面及上机作业
2. 注意先修课程的知识准备：离散数学、C语言
3. 注意循序渐进：
基本概念、基本思想、基本步骤、算法设计
4. 注意培养算法设计的能力
理解所讲算法、对此多做思考：若问题要求不同，应如何选择数据结构，设计有效的算法
课程特点：内容抽象、概念性强、内容灵活、不易掌握

---

## 第 6 页

数据结构课程的形成和发展：
形成阶段：
60年代初期，“数据结构”有关的内容散见于操作系统、编译原理和表处理语言等课程。1968年，“数据结构”被列入美国一些大学计算机科学系的教学计划。
发展阶段：
数据结构的概念不断扩充，包括了网络、集合代数论、关系等“离散数学结构”的内容。
70年代后期，我国高校陆续开设该课程。

---

## 第 7 页

《数据结构》所处的地位：
介于数学、计算机硬件和计算机软件三者之间的一门核心课程

---

## 第 8 页

数据结构在计算机学科中的地位

---

## 第 9 页

基础：掌握常用数据结构的概念
基础：掌握常用数据结构的不同实现方法

技能：通过系统学习在不同存储结构上的运算方法，能够在不同存储结构上实现不同的运算，并对算法设计的方式和技巧有所体会。
课程目标

---

## 第 10 页

能够分析研究计算机处理的对象的特性，获得其逻辑结构，根据需求，选择合适存储结构及其相应的算法；
学习一些常用的算法；
复杂程序设计的训练过程，要求编写的程序结构清楚和正确易读；
初步掌握算法的时间复杂度和空间复杂度分析过程
课程目标

---

## 第 11 页

1.1 数据结构的研究内容
1.2  基本概念和术语
1.3  抽象数据类型的表示与实现
1.4  算法与算法分析
课程内容

---

## 第 12 页

1.1 　数据结构的研究内容
N.沃思（Niklaus  Wirth)提出：程序=算法+数据结构
1. 算法（Algorithm）：是解决问题的步骤或方法，它定义了如何通过一系列操作来处理输入数据并产生输出结果。
算法的设计需要考虑解决问题的正确性和效率（时间复杂度和空间复杂度）等。
2. 数据结构（Data Structure）：是数据的组织、管理和存储格式，它决定了数据如何被存储和访问。
3. 算法与数据结构的关系：算法的执行效率往往依赖于所使用的数据结构，而数据结构的设计也需要考虑算法的需求。

---

## 第 13 页

1.1 　数据结构的研究内容
电子计算机的主要用途：
早期：
      主要用于数值计算，数学计算算法的研究
后来：
      处理逐渐扩大到非数值计算领域，能处理多种复杂的具有一定结构关系的数据
此时，出现了数据结构的大量基础性的研究

---

## 第 14 页

书目自动检索系统

---

## 第 15 页

学生成绩表

---

## 第 16 页

人机对奕问题

---

## 第 17 页

/ (root)
bin
lib
user
etc
math
ds
sw
yin
tao
xie
Stack.cpp
Queue.cpp
Tree.cpp
文件系统的系统结构图

---

## 第 18 页

田径比赛

---

## 第 19 页

社交关系网络

---

## 第 20 页

求解非数值计算的问题：
    设计出合适的数据结构及相应的算法
即：首先要考虑对相关的各种信息如何表示、组织和存储？

数据结构的研究内容为：
研究非数值计算的程序设计问题中计算机的操作对象以及它们之间的关系和操作。
程序=算法+数据结构

---

## 第 21 页

1.2 　基本概念和术语
数据（data）是客观事物的符号表示，是所有能输入到计算机中并被计算机程序处理（输入、存储、运算、输出）的符号的总称。
任何需要被计算机处理的数据，首先要做的事情就是符号化：
数值：整数（原码、补码）、实数（定点数、浮点数）
字符：西文字符（ASCII码）、中文字符（Unicode，UTF）
图像：采用像素化的方式，进行采样、量化和编码
声音：采用时间分片的方式，进行PCM式采样、量化、编码

---

## 第 22 页

1.2 　基本概念和术语
数据分为：数值数据和非数值数据
数值数据典型应用是数值计算：
指有效使用数字计算机求数学问题近似解的方法与过程。比如连续系统离散化和离散形方程求解，需要考虑误差、收敛性和稳定性等问题。
数值运算的研究领域包括数值逼近、数值微分和数值积分、数值代数、最优化方法、常微分方程数值解法、积分方程数值解法、偏微分方程数值解法、计算几何、计算概率统计等。随着计算机的广泛应用和发展，许多计算领域的问题，如计算物理、计算力学、计算化学、计算经济学等都可归结为数值计算问题。
主要是运用有限逼近的思想来进行误差运算

---

## 第 23 页

1.2 　基本概念和术语
非数值数据（字符、文字、图形、图像、声音等）
例子：图像的位图表示形式
在计算机中, 图像是由一个个像素点组成，像素点就是颜色点，而颜色最简单的方式就是用RGB或RGBA表示, 如图所示

---

## 第 24 页

数据 >数据元素>数据项
数据由多个数据元素按照一种或多种关系组成，单个数据元素往往由多个数据项组成。
例：学生表 >  个人记录 >  学号、姓名……
1.2 　基本概念和术语
数据元素（data element）是数据的基本单位；在计算机中通常做为一个整体进行考虑和处理；一个数据元素可以由若干个数据项组成。
数据项（data item）是具有独立含义的表示单位，是数据的不可分割的最小单位；数据项可以是组合项（可再由若干数据元素组成）。
数据（data）是对客观事物的符号表示；是能被计算机识别、存储、和加工处理的符号的总称。

---

## 第 25 页

1.2 　基本概念和术语
数据：成绩表
数据元素：表中的一行
数据项：组成表中一行的每个字段
数据：田径项目的组成关系树
数据元素：树中的结点
数据项：结点可能有的属性
数据：人际关系图
数据元素：图中的结点
数据项：结点可能有的属性，如姓名、年龄等

---

## 第 26 页

数据对象(Data Object)：相同特性数据元素的集合，是数据的一个子集
整数数据对象
         N = { 0, 1, 2, … }
学生数据对象
学生记录的集合

---

## 第 27 页

数据结构（data struct）是指相互之间存在着一种或多种特定关系的数据元素的集合。
 包括：
多个数据元素间的逻辑关系，数据的逻辑结构：线性结构、树形结构、图形结构
数据在计算机中的实际存储方式，数据的存储结构：顺序存储、链式存储、索引存储、散列存储
在这些数据上定义的一组运算，数据的运算或操作：引用型运算、加工型运算

---

## 第 28 页

数据结构的两个层次：
逻辑结构---
数据元素间抽象化的相互关系，与数据的存储无关，独立于计算机，它是从具体问题抽象出来的数学模型。
存储结构（物理结构）----
数据元素及其关系在计算机存储器中的存储方式。

---

## 第 29 页

划分方法一：
按结点间的对应关系来分

（1）线性结构----（1对1）
有且仅有一个开始和一个终端结点，并且所有结点都最多只有一个直接前趋和一个后继。
例如：线性表、栈、队列、串
（2）非线性结构----（1对多或1对0）
一个结点可能有多个或0个直接前趋和直接后继。
例如：树、图、集合
逻辑结构

---

## 第 30 页

逻辑结构
线性结构：一个对一个，有逻辑的先后关系，
包括：线性表、栈、队列等；
如，一根链条，一个单词中的所有字母
树形结构：一个对多个，
包括：树、二叉树、森林；
如，一棵树，家谱树等
集合：数据元素间除“同属于一个集合”外，无其它关系，如，一堆沙子
图形结构：多个对多个，图
如，社交关系图
划分方法二：

---

## 第 31 页

逻辑结构总结

---

## 第 32 页

存储结构分为：
顺序存储结构——借助元素在存储器中的相对位置来表示
                               数据元素间的逻辑关系
链式存储结构——借助指示元素存储地址的指针表示数据
                               元素间的逻辑关系
存储结构

---

## 第 33 页

存储结构
顺序存储结构：数据元素依次放在连续的存储单元中
借助元素在存储器中的相对位置来表示数据元素间的逻辑关系，相对位置可通过计算得到

链式存储结构：借助指示元素存储地址的指针表示数据元素间的逻辑关系，在存储结点中增加若干指针域，记录后继或者相关结点的地址（指针）
a1
a2
a19
an
结点
00   04             72         4*(n-1)
   00   04                      72   76               220 224
指针
a1 220
a2 336
a2  72
结点

---

## 第 34 页

---

## 第 35 页

1536
元素2
1400
元素1
1346
元素3
 ∧
元素4
1345
h
存储地址 | 存储内容 | 指针
--- | --- | ---
1345 | 元素1 | 1400
1346 | 元素4 | ∧
……. | …….. | …….
1400 | 元素2 | 1536
……. | …….. | …….
1536 | 元素3 | 1346
 链式存储
h

---

## 第 36 页

数据的基本运算
插入（增）
删除（删）
修改（改）
查找（查）
排序（有时在增、删、改的过程中一起完成）
逻辑结构和存储结构都相同, 但运算不同, 则数据结构不同。例如, 栈与队列（都是线性表，但栈的运算在逻辑结构的第一个位置进行，队列可以在第一个、最后一个及某个指定位置进行）。
对于一种数据结构,基本运算指的是逻辑结构上的运算（操作），包括：

---

## 第 37 页

 数据的逻辑结构
 数据的存储结构
数据的运算：插入、删除、修改、查找、排序
 线性结构
 非线性结构
 顺序存储
  链式存储
线性表
      栈、队列
           串、数组
树形结构
图形结构
逻辑结构
唯一

存储结构
不唯一

运算的实现
依赖于
存储结构

---

## 第 38 页

定义：在一种程序设计语言中，变量所具有的数据种类
数据类型
FORTRAN语言：整型、实型、和复数型
C语言：
　基本数据类型： char  int  float  double  void
　构造数据类型：数组、结构体、共用体、文件
数据类型是一组性质相同的值的集合, 以及定义于这个集合上的一组运算的总称

---

## 第 39 页

抽象数据类型(ADTs: Abstract  Data Types)
更高层次的数据抽象
由用户定义，用以表示应用问题的数据模型
由基本的数据类型组成, 并包括一组相关的操作

                    数据的逻辑结构+运算的定义-------面向用户
                       （抽象数据类型）                         概念层
                    数据的存储结构+运算的实现-------面向计算机
                                                                                实现层
1.3 抽象数据类型的表示与实现

---

## 第 40 页

抽象数据类型可以用以下的三元组来表示：
                ADT = （D，S，P）

                数据对象   D上的关系集    D上的操作集
ADT抽象数据类型名{
        数据对象：<数据对象的定义>
        数据关系：<数据关系的定义>
        基本操作 ：<基本操作的定义>
    } ADT抽象数据类型名
ADT常用定义格式

---

## 第 41 页

信息隐蔽和数据封装，使用与实现相分离
抽象数据类型
检索图书
借书
还书
续借图书
学生信息
图书信息修改
学生离校
进新书
查找　插入　  删除    修改
顺序存储的图
二叉链表存储的二叉树
顺序存储的线性表
接口或用户界面（API，Application Programming Interface，应用程序接口）
数据类型的物理实现封装

---

## 第 42 页

1.3 　抽象数据类型的表示与实现
抽象数据类型可以通过固有的数据类型（如整型、实型、字符型等）来表示和实现。
它有些类似C语言中的结构（struct)类型，但增加了相关的操作
教材中用的是类C语言（介于伪码和C语言之间）作为描述工具
但上机时要用具体语言实现，如C或C++等

---

## 第 43 页

算法定义：一个有穷的指令集，这些指令为解决某一特定任务规定了一个运算序列（实现步骤）
在数据结构课程中，狭义的算法是指在存储结构上实现数据的逻辑结构所必须的基本运算的代码序列。
通常在计算机的问题解决中，我们是针对问题设计一组数据描述的逻辑结构和运算方法，并通过设计算法来实现数据的运算，从而最终解决掉问题。
算法的描述：
 自然语言
 流程图
 程序设计语言
 伪码
1.4 　算法和算法分析

---

## 第 44 页

  算法的特性：
有穷性   一个算法必须总是在执行有穷步之后结束，且每一步都在有穷时间内完成。
确定性  算法中每一条指令必须有确切含义。不存在二义性。且算法只有一个入口和一个出口。
可行性  一个算法是可行的。即算法描述的操作都是可以通过已经实现的基本运算执行有限次来实现的。
输入  一个算法有零个或多个输入，这些输入取自于某个特定的对象集合。
输出  一个算法有一个或多个输出，这些输出是同输入有着某些特定关系的量。

---

## 第 45 页

算法的评价
正确性
可读性
健壮性
高效性（效率与低存储量需求）
时间代价，效率
空间代价，存储量使用需求
这两个是互相影响的，要找到针对被解决的问题的最佳平衡点
算法有好坏之分！即通过几个方面来对算法进行评价

---

## 第 46 页

算法的评价
算法有好坏之分！即通过几个方面来对算法进行评价
求10个数的最大值。
main()
{
int max=0,x ,n=10;
for(int i=1;i<=n;i++)
{ scanf(“%d”,&x);
if(x>max) max=x;}
printf(“%d”,max);
}
提示：本程序无语法错误

格式不好，不满足算法的“可读性”要求。

---

## 第 47 页

算法的评价
求10个数的最大值。
main()
{
    int max=0,x,i,n=10;
    for(i=1;i<=n;i++)
    {
         scanf(“%d”,&x);
         if(x>max)
         {
             max=x;
         }
    }
    printf(“%d”,max);
}
提示：本程序无语法错误

当输入全为正数时，结果正确；
当输入全为负数时，求得的最大值为0，结果不正确。
既要输入正确的数据，也要输入错误的数据进行健壮性测试。

不满足算法的“正确性”要求。

---

## 第 48 页

算法效率：用依据该算法编制的程序在计算机上执行所消耗的时间来度量
算法效率的度量
事后统计
事前分析估计

---

## 第 49 页

算法效率的度量
不同算法的比较，求1+2+3+…+100的程序。
main()
{
    int i,s=0;n=100;
    s=(1+n)*n/2;
    printf("%d",s);
}
main()
{
    int i,s=0;n=100;
    for(i=1;i<=100;i++)
          s=s+i;
    printf("%d",s);
}
s = 1+    2+     3+  ...+99  +100
s =100+99+   98+ ...+2    +  1
2s=101+101+101+...+101+101
这是数学家高斯的解释，(1+100)*100/2，就是加到一千一万，也能很快口算出来。

---

## 第 50 页

算法效率的度量
例子---保持队列连续
一开始队列是空的，经过6个步骤，队列满了

---

## 第 51 页

算法效率的度量
例子---保持队列连续
再经过1个步骤，队列中的“Nancy”离开了。

---

## 第 52 页

算法效率的度量
例子---保持队列连续
问题：如何保持队列连续？

方法1：依次向前移动一个位置。

---

## 第 53 页

算法效率的度量
例子---保持队列连续
问题：如何保持队列连续？

                                            方法2：将最后一个挪过来。

---

## 第 54 页

算法效率的度量
例子---保持队列连续
问题：如何保持队列连续？

方法1：依次向前移动一个位置。     方法2：将最后一个挪过来。
同样的逻辑结构，
同样的运算，
算法不同！

---

## 第 55 页

算法效率的度量
例子---保持队列连续
问题：如何保持队列连续？

方法1：依次向前移动一个位置。     方法2：将最后一个挪过来。
同样的逻辑结构，
同样的运算，
算法不同！
空间代价相同；
时间代价不同；
问题修改为：保持队列连续并且队员间的相互顺序不变？如何解决？

---

## 第 56 页

1.事后统计：利用计算机内的计时功能，不同算法的程序可以用一组或多组相同的统计数据区分
缺点：
必须先运行依据算法编制的程序
所得时间统计量依赖于硬件、软件等环境因素，掩盖算法本身的优劣
如何克服缺点2：
用多种硬件、多种软件、多种数据进行多次组合测试 ----性能测试

---

## 第 57 页

图片来自《算法之道》P20

---

## 第 58 页

2.事前分析估计：
一个高级语言程序在计算机上运行所消耗的时间取决于：
     依据的算法选用何种策略
     问题的规模
     程序语言
     编译程序产生机器代码质量
     机器执行指令速度
同一个算法用不同的语言、不同的编译程序、在不同的计算机上运行，效率均不同，———使用绝对时间单位衡量算法效率不合适

---

## 第 59 页

算法效率的度量
不同算法的比较，求1+2+3+…+100的程序。
main()
{
    int i,s=0;n=100;
    s=(1+n)*n/2;
    printf("%d",s);
}
main()
{
    int i,s=0;n=100;
    for(i=1;i<=100;i++)
          s=s+i;
    printf("%d",s);
}
s = 1+    2+     3+  ...+99  +100
s =100+99+   98+ ...+2    +  1
2s=101+101+101+...+101+101
这是数学家高斯的解释，(1+100)*100/2，就是加到一千一万，也能很快口算出来。

---

## 第 60 页

算法性能分析与度量
main()
{
    int i,s=0;n=100;
   //执行1次
    s=(1+n)*n/2;
   //执行1次
    printf("%d",s);
 //执行1次
}
//共执行3次
main()
{
    int i,s=0;n=100;
    //执行1次
    for(i=1;i<=100;i++)
   //分别执行1次、n+1次、n次
          s=s+i; //执行n次
    printf("%d",s);
   //执行1次
}
//共执行3n+4次
两个算法的第一条、最后一条语句是一样的，中间部分是我们要关注的，把循环当作一个整体，忽略头尾循环判断的次数，这两个算法的差别就是1与n的区别。

---

## 第 61 页

算法的评价
正确性
可读性
健壮性
高效性（效率与低存储量需求）
时间代价，效率
空间代价，存储量使用需求
这两个是互相影响的，要找到针对被解决的问题的最佳平衡点
算法有好坏之分！即通过几个方面来对算法进行评价

---

## 第 62 页

算法效率：用依据该算法编制的程序在计算机上执行所消耗的时间来度量
算法效率的度量
事后统计
事前分析估计

---

## 第 63 页

1.4 　算法和算法分析
时间复杂度：一个算法执行所耗费的时间是所有语句执行时间之和，用T(n)表示，假设每个语句执行时间相同时（用ti表示），只需要统计所有语句执行的次数总和即可
 时间频度：一个算法中的原操作执行次数称为语句频度或时间频度，第i条语句的时间频度用 ci 表示；

---

## 第 64 页

1.4 　算法和算法分析

算法1：
int i, sum =0, n=100;
for(i=1;i<=n;i++)
{
	sum=sum+i;
}

总共执行？
3n+3次

---

## 第 65 页

1.4 　算法和算法分析

算法2：
int i, sum=0, n=100; //执行1次
sum =(1+n)*n/2; //执行1次

总共执行2次

---

## 第 66 页

1.4 　算法和算法分析
对于问题输入规模n，四种算法执行的次数为：
2n+3
2n
3n+1
3n

哪个时间复杂度低？
次数 | 2n+3 | 2n | 3n+1 | 3n
--- | --- | --- | --- | ---
n=1 | 5 | 2 | 4 | 3
n=2 | 7 | 4 | 7 | 6
n=3 | 9 | 6 | 10 | 9
n=10 | 23 | 20 | 31 | 30
n=100 | 203 | 200 | 301 | 300
5>4
203<301

---

## 第 67 页

1.4 　算法和算法分析
对于问题输入规模n，三种算法执行的次数为：
2n2
3n+1
2n2+3n+1
次数 | 2n2 | 3n+1 | 2n2+3n+1 | 10n
--- | --- | --- | --- | ---
n=1 | 2 | 4 | 6 | 10
n=2 | 8 | 7 | 15 | 20
n=5 | 50 | 16 | 66 | 50
n=10 | 200 | 31 | 231 | 100
n=100 | 20000 | 301 | 20301 | 1000
哪个时间复杂度低？

---

## 第 68 页

算法中基本语句重复执行的次数是问题规模n的某个函数f(n),算法的时间量度记作：
T(n)=O(f(n))
时间复杂度的渐进表示法
数学符号“O”的定义为：
若T(n)和f(n)是定义在正整数集合上的两个函数，则T(n) = O(f(n))表示存在正的常数C和n0，使得当n≥n0时都满足0≤T(n)≤Cf(n)。
表示随着n的增大，算法执行的时间的增长率和f(n)的增长率相同，称渐近时间复杂度。
n越大算法的执行时间越长
排序：n为记录数
矩阵：n为矩阵的阶数
多项式：n为多项式的项数
集合：n为元素个数
树：n为树的结点个数
图：n为图的顶点数或边数
算法中重复执行次数和算法的执行时间成正比的语句
对算法运行时间的贡献最大
f(n)通常选取容易衡量的函数： log2n，n， log2n ，n2，n3 ，2n

---

## 第 69 页

例1  分析以下程序段的时间复杂度
	for (i=1;i<n;i++) {
	     y=y+1;
	     for (j=0; j<=(2*n); j++)
		x++;
	   }
//n-1
该程序段的时间复杂度：
		T(n)=

---

## 第 70 页

推导大O阶的方法
1）程序运行时间中的常数用1代替。如运行3次，记为O(1), 运行100次，也记为O(1)。
2）在大O()函数中运行次数只取最高阶项，并且最高阶项的常数为1。如5n2记O(n2)。
它表示随问题规模n的增大算法的执行时间的增长率和f(n)的增长率相同，称作算法的渐进时间复杂度，简称时间复杂度。
一般情况下，随n的增大，T(n)的增长较慢的算法为最优的算法。

---

## 第 71 页

【例】在下列三段程序段中，给出原操作x=x+1的时间复杂度分析。
（1）x=x+1 ；程序执行1次，其时间复杂度为O(1)
		     称之为常量阶；
（2）for (i=1; i<= n; i++)  //执行n+1次
                     x=x+1; 	//执行n次
           程序共执行2n+1, 只取最高项，去掉该项的系数，
            T(n)=O(n)，称之为线性阶；
（3）for (i=1; i<= n; i++)  //执行n+1次
          for (j=i;j<= n; j++)  //执行n(n+1) 次
          x=x+1; 	        //执行n2次,
         共执行2n2+2n+1, 只取最高项，去掉该项的系数，
         T(n)=O(n2), 称之为平方阶。

---

## 第 72 页

n * n阶矩阵加法：
for( i = 0; i < n; i++)
	for( j = 0; j < n; j++)
		c[i][j] = a[i][j] + b[i][j];
语句的频度（Frequency Count ): 重复执行的次数：n*n;
T( n ) = O ( n 2)
即：矩阵加法的运算量和问题的规模n的平方是同一个量级

---

## 第 73 页

找出语句频度最大的那条语句作为基本语句
计算基本语句的频度得到问题规模n的某个函数f(n)
取其数量级用符号“O”表示
分析算法时间复杂度的基本方法
f(n)=n2
T(n) = O(n2)

---

## 第 74 页

 void exam ( float x[ ][ ], int m, int n ) {
     float sum [ ];
     for ( int i = 0; i < m; i++ ) {
         sum[i] = 0.0;
         for ( int j = 0; j < n; j++ )
	    sum[i] += x[i][j];
     }
     for ( i = 0; i < m; i++ )
         cout << i << “ : ” <<sum [i] << endl;
 }
时间复杂度是由嵌套最深层语句的频度决定的
f(n)=m*n
T(n) = O(m*n)

---

## 第 75 页

例1：N×N矩阵相乘
for(i=1;i<=n;i++) // 外层循环，运行 n 次
  for(j=1;j<=n;j++) // 中层循环，运行 n 次,总共运行n×n=n2
     {c[i][j]=0;	// 常数时间操作O(1)
       for(k=1;k<=n;k++) // 内层循环，运行 n 次,总共运行n× n2 =n3
          c[i][j]=c[i][j]+a[i][k]*b[k][j]; // 常数时间操作O(1)
     }

---

## 第 76 页

例2：
for( i=1; i<=n; i++)
         for (j=1; j<=i; j++)
             for (k=1; k<=j; k++)
　　　　x=x+1;
语句频度 =
定理1.1
若f(n)=amnm+am-1nm-1++a1n+a0是m次多项式，则T(n)=O(nm)。
忽略所有低次幂项和最高次幂系数，体现出增长率的含义

---

## 第 77 页

例3：分析以下程序段的时间复杂度
i=1;                              ①
while(i<=n)
	 i=i*2;                      ②
即f(n)≤log2n，取最大值f(n)=log2n
所以该程序段的时间复杂度T(n) =O( log2n)
循环的执行过程
初始值：i=1
每次迭代：i=2×i
循环条件：i≤n
循环的执行过程如下：
第 1 次迭代：i=1
第 2 次迭代：i=2
第 3 次迭代：i=4
...
第 k 次迭代：i=2k−1
循环会在 i>n 时终止
因此我们需要找到满足 2k−1≤n 的最大 k。
取对数： k -1≤log2n;
循环的执行次数为O(log2n)

---

## 第 78 页

例4：顺序查找，在数组a[i]中查找值等于e的元素，返回其所在位置。
  for (i=0;i< n;i++)
```c
      if (a[i]==e) return i+1;
          return 0;
```
有的情况下，算法中基本操作重复执行的次数还随问题的输入数据集不同而不同
最好情况：1次
最坏情况：n
平均时间复杂度为:O(n)

---

## 第 79 页

时间复杂度T(n)按数量级递增顺序为：
复杂度高
复杂度低
　当n取得很大时，指数时间算法和多项式时间算法在所需时间上非常悬殊

---

## 第 80 页

空间复杂度:算法所需存储空间的度量，记作:    S(n)=O(f(n))
其中n为问题的规模(或大小)
算法要占据的空间
算法本身要占据的空间，输入/输出，指令，常数，变量等
算法要使用的辅助空间
渐进空间复杂度

---

## 第 81 页

【算法1】
for(i=0;i<n/2;i++)
{   t=a[i];
    a[i]=a[n-i-1];
    a[n-i-1]=t;
}
【算法2】
for(i=0;i<n;i++)
    b[i]=a[n-i-1];
for(i=0;i<n;i++)
    a[i]=b[i];
例5：将一维数组a中的n个数逆序存放到原数组中。
S(n) = O(n)
S(n) = O(1)
原地工作
除了输入数组外，只需要常数级别的额外空间（用于存储临时变量t和索引变量 i)。
除了输入数组外，还需要开辟与问题规模n相同的额外空间

---

## 第 82 页

1、数据、数据元素、数据项、数据结构等基本概念
2、对数据结构的两个层次的理解
逻辑结构
存储结构
3、抽象数据类型的表示方法
4、算法、算法的时间复杂度及其分析的简易方法
小结

---
