第 5 步：主动练习
现在用伪代码 + C 代码完成 BST delete。

题目：假设有如下 BST 节点结构，写出 deleteNode 函数。


struct TreeNode {
    int val;
    struct TreeNode *left;
    struct TreeNode *right;
};

// 返回删除指定值后新的根节点
struct TreeNode* deleteNode(struct TreeNode* root, int val)
{
    if (val < root->val)
    {
        return (root->left);
    }

    else if (val > root->val)
    {
        return (root->right);
    }

    else
    {
        if(root->left)
    }
}
要求：

先写出三种情况的伪代码（中文或伪代码都可以）
再写出完整的 C 代码（含 findMin 辅助函数找最小值节点）
特别注意：每个删除都必须 free 被删节点
起点提示：先写查找逻辑——如果 val < root->val，在左子树删除；如果 val > root->val，在右子树删除；如果相等，找到了，开始三种情况判断。